# Vercel Blob URL of a pre-uploaded Facebook Messenger export zip.
# When set, the upload page is skipped and the zip is downloaded and processed automatically.
# Upload your zip to Vercel Blob (https://vercel.com/docs/storage/vercel-blob) and paste the URL here.
# Leave empty to use the manual upload mode instead.
NUXT_BLOB_ZIP_URL=
